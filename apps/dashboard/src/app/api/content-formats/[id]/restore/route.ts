import { NextResponse } from "next/server"; import { ContentFormatConflictError, ContentFormatNotFoundError, restoreContentFormat } from "@/server/content-formats";
export async function POST(_: Request, { params }: { params: { id: string } }) { try { return NextResponse.json(await restoreContentFormat(params.id)); } catch (e) { return NextResponse.json({ error: e instanceof Error ? e.message : "Internal Server Error" }, { status: e instanceof ContentFormatNotFoundError ? 404 : e instanceof ContentFormatConflictError ? 409 : 500 }); } }
