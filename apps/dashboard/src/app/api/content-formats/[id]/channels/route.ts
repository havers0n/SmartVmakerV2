import { NextResponse } from "next/server"; import { ZodError } from "zod"; import { ContentFormatConflictError, attachChannelToContentFormat } from "@/server/content-formats";
export async function POST(r: Request, { params }: { params: { id: string } }) { try { return NextResponse.json(await attachChannelToContentFormat(params.id, await r.json()), { status: 201 }); } catch (e) { return NextResponse.json({ error: e instanceof ZodError ? "Invalid channel association" : e instanceof ContentFormatConflictError ? e.message : "Internal Server Error" }, { status: e instanceof ZodError ? 400 : e instanceof ContentFormatConflictError ? (e.message.includes("not found") ? 404 : 409) : 500 }); } }
