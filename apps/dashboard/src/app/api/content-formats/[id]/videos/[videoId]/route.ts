import { NextResponse } from "next/server"; import { ZodError } from "zod"; import { ContentFormatConflictError, detachVideoFromContentFormat, updateVideoAssociation } from "@/server/content-formats";
function response(e: unknown) { return NextResponse.json({ error: e instanceof ZodError ? "Invalid video association" : e instanceof ContentFormatConflictError ? e.message : "Internal Server Error" }, { status: e instanceof ZodError ? 400 : e instanceof ContentFormatConflictError ? (e.message.includes("not found") ? 404 : 409) : 500 }); }
export async function PATCH(r: Request, { params }: { params: { id: string; videoId: string } }) { try { return NextResponse.json(await updateVideoAssociation(params.id, params.videoId, await r.json())); } catch (e) { return response(e); } }
export async function DELETE(_: Request, { params }: { params: { id: string; videoId: string } }) { try { await detachVideoFromContentFormat(params.id, params.videoId); return new NextResponse(null, { status: 204 }); } catch (e) { return response(e); } }
