import { NextResponse } from "next/server"; import { ZodError } from "zod"; import { ContentFormatConflictError, attachVideoToContentFormat } from "@/server/content-formats";
export async function POST(request: Request, { params }: { params: { id: string } }) { try { return NextResponse.json(await attachVideoToContentFormat(params.id, await request.json()), { status: 201 }); } catch (e) { return NextResponse.json({ error: e instanceof ZodError ? "Invalid video association" : e instanceof ContentFormatConflictError ? e.message : "Internal Server Error" }, { status: e instanceof ZodError ? 400 : e instanceof ContentFormatConflictError ? (e.message.includes("not found") ? 404 : 409) : 500 }); } }
